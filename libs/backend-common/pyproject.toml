[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "backend-common"
version = "0.1.0"
description = "Shared backend utilities for WorkoutApp"
requires-python = ">=3.12,<4.0"

[tool.setuptools]
packages = ["backend_common"]
